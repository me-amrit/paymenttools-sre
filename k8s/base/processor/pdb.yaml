apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: payment-processor
  namespace: payment
  labels:
    app: payment-processor
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: payment-processor
