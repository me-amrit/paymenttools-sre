apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: payment-gateway
  namespace: payment
  labels:
    app: payment-gateway
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: payment-gateway
